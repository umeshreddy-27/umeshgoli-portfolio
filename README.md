# Umesh Goli Portfolio (Creative Modern)
This repository contains a React + Vite + Tailwind portfolio site with Framer Motion animations and EmailJS contact form.

## Quick steps
1. Rename `.env.example` to `.env.local` and fill in EmailJS keys.
2. npm install
3. npm run dev (preview) or deploy to Vercel following README_deploy.md
